UI:

- Create notice/update/event widget in the main screen

FIX UI:

- Fix scrollarea bottem left and right rounded corners (button section below looks separated)

LOGIC:

- In the game fix the dumb way of waiting with setInterval to be able to acces the objects in the scene
- isActive status of hotbar items visual missing
- Change drone type and its stats - update it in the UI over the player
- Load last used formation for
- Fix dronedesign placement and sync with VUEX store (design coming from a drone to inventory, from inventory to inventory, etc...)
- Cant open same item in shop twice, since the popup doesn't register the itemID change from f.e. 5->5
